import { ListCustomersDirective } from './list-customers.directive';

describe('ListCustomersDirective', () => {
  it('should create an instance', () => {
    const directive = new ListCustomersDirective();
    expect(directive).toBeTruthy();
  });
});
